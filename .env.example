# Copy to .env in this directory and edit. Used by bootstrap, redeploy, and challenge registration.
#
# Optional: SQLite URI for the catering agentic SQL lab only (default: catering_sql_lab.db next to pizza_shop.db).
# CATERING_SQL_DATABASE_URI=sqlite:////absolute/path/to/catering_sql_lab.db

# Required — hostname or IP participants type in the browser to open spawned apps (no http://).
# Example (EC2): ec2-203-0-113-50.compute-1.amazonaws.com
# Example (local testing on one machine): 127.0.0.1
DOCKER_CHALLENGES_PUBLIC_HOST=

# Optional alias if you prefer this name (scripts copy it to DOCKER_CHALLENGES_PUBLIC_HOST).
# PWNZZAI_PUBLIC_HOST=

# docker_challenges plugin — cloned when building the CTFd image (defaults = northdpole fork with fixes).
# DOCKER_CHALLENGES_GIT_URL=https://github.com/northdpole/CTFd-Docker-Challenges.git
# DOCKER_CHALLENGES_REF=master
# Optional: cache-buster for plugin clone step (redeploy script auto-sets this each run).
# DOCKER_CHALLENGES_CACHE_BUSTER=

# Recommended for production CTFd
# CTFD_SECRET_KEY=

# Compose: start the local shared Ollama container. Leave empty to run CTFd only and use a remote model host.
# COMPOSE_PROFILES=local-ollama
#
# Workshop compose only: Ollama NVIDIA GPU file (docker-compose.workshop.nvidia.yml). On CPU-only VMs (e.g. most
# DigitalOcean droplets) leave unset so Docker does not request a GPU (avoids CDI "no known GPU vendor" errors).
# PWNZZAI_OLLAMA_GPU=0
# PWNZZAI_OLLAMA_GPU=1
#
# DigitalOcean bootstrap only: allow SSH password login (root password from provider). Less secure than keys.
# PWNZZAI_SSH_PASSWORD_AUTH=1
#
# Bind address for published Ollama port (default all interfaces). Tighten in production.
# OLLAMA_PUBLISH=0.0.0.0:11434

# Shared model/provider contract for spawned challenge containers.
# `MODEL_PROVIDER=auto` prefers OpenAI when a key is available, then falls back to Ollama.
# MODEL_PROVIDER=auto
# OLLAMA_HOST=http://172.17.0.1:11434
# OLLAMA_MODEL=mistral:7b
# OLLAMA_FALLBACK_MODEL=llama3.2:1b
# OPENAI_API_KEY=
# Bare OpenAI model id (app builds openai/{OPENAI_MODEL}). No default in code — set for OpenAI cloud labs.
# OPENAI_MODEL=gpt-4o-mini
#
# Google Gemini: bare model id (e.g. gemini-3.1-flash-lite) becomes gemini/<id>, or set full route if value contains '/'.
# No default in code. Used when LITELLM_MODEL is unset; also fills LAB_CLOUD_* when those are unset.
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-3.1-flash-lite
#
# Full LiteLLM route; wins over GEMINI_MODEL / OPENAI_MODEL for UI and resolved default.
# LITELLM_MODEL=gemini/gemini-3.1-flash-lite
#
# Per-lab cloud model overrides (optional). Bare names get openai/ prefix; use provider/model for Claude, Gemini, etc.
# LAB_CLOUD_LLM_MODEL=
# LAB_CLOUD_LLM_MODEL_EXCESSIVE_AGENCY=
# See docs/workshop-cloud-llm-setup.md for step-by-step examples.
# Optional UI overrides when using a non-OpenAI backend:
# LLM_UI_PROVIDER_NAME=
# LLM_UI_KEY_LABEL=
# LLM_UI_KEY_PLACEHOLDER=
# LLM_UI_DOCS_URL=
# LLM_UI_DOCS_ANCHOR=
# LLM_UI_LAB_HEADING=
# LLM_UI_LAB_DESCRIPTION=
# ALLOW_PRECONFIGURED_OPENAI_KEY=true
# PREFER_SESSION_OPENAI_KEY=true
# MODEL_TIMEOUT_SECONDS=60
# ENABLE_PROVIDER_FALLBACK=true
